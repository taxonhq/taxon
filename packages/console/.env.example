# Taxon service URL
NEXT_PUBLIC_TAG_SERVICE_URL="http://localhost:3300"

# Day-boundary timezone offset in minutes east of UTC for "today" stats (#148).
# Must match the service's APP_TZ_OFFSET_MIN. Default 0 = UTC; UTC+8 → 480.
# NEXT_PUBLIC_APP_TZ_OFFSET_MIN=480

# API Token（可选）
# ⚠️ 安全警告（#137）：NEXT_PUBLIC_ 前缀的变量会被 Next.js 内联进**浏览器 bundle**，
#    任何能打开控制台或拿到其 JS 的人都能在 DevTools 看到明文 token。
#    - 仅限纯本地 / 可信网络的开发场景使用；切勿在公网部署中填写 admin token。
#    - 生产部署建议：经 Next.js 服务端（Route Handler / server action）转发请求，
#      token 留在服务端 env（不加 NEXT_PUBLIC_ 前缀），浏览器永不接触；
#      或为控制台签发最小权限（reader/reviewer）token，而非 admin。
# NEXT_PUBLIC_TAG_SERVICE_TOKEN=""
